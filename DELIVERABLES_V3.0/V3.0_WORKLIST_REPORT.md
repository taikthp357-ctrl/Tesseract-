# V3.0 CP1_WORKLIST — Report

**Date:** 2026-08-22
**Checkpoint:** `work\WORKING\260821_CF_OB_V3.0_CP1_WORKLIST.xlsm`
**SHA256:** `987CB500BE1DB519E943B924F5D1CF7BB8AA1CCDAFD3B80BC7D8BD9E31EA06D6`
**Baseline (untouched):** `260821_CF_OB_V3.0.xlsm` SHA `B0A354CB1F247517B5C1251C5B4F68ED6C2FDD216DEBF46B65A8588A6545918A`
**Build script:** `work\scripts\v30_cp1_worklist.ps1`
**Log:** `work\REPORTS\v30_cp1_worklist.log`

## Scope

Two operator-requested worklist column changes, contained to the two UI modules
(no engine logic touched):

1. **After `Whse Order`, insert the "mã cần sửa" columns** — the user chose *Cả hai
   cột* (BOTH): `G = WT` (DATA_CORE col 5) and `H = Material` (col 6).
2. **`tình trạng` → raw status** — column now shows only blank or `A`
   (`C`/OB_DONE/WT_DONE rows are already filtered out of the worklist).

### Final worklist layout (CF_CONTROL, headers row 8, data 9–200)

| Col | Header | Source | State |
|-----|--------|--------|-------|
| E | OB | DC_OB(1) | locked |
| F | Whse Order | DC_WHO(4) | locked |
| G | WT (mã cần sửa) | DC_WT(5) **NEW** | locked |
| H | Material | DC_MATERIAL(6) **NEW** | locked |
| I | Tình trạng (blank/A) | raw DC_STATUS(10) | locked |
| J | Kế hoạch | WL_PlanText | locked |
| K | Gate | WL_GateText | locked |
| L | SL thực tế | DC_ACTUAL_QTY(12) | **yellow/editable** |
| M | Khu vực | DC_AREA(11) | **yellow/editable** |
| N | Số bin | DC_BIN_NO(13) | **yellow/editable** |
| O | Kết quả | WL_ResultText (VN) | locked |
| P | Chi tiết | DC_ERROR(19) | locked |
| Q | (hidden Source Key) | DC_SOURCE_KEY(20) | hidden |

## Verification — all offline gates PASS

| Gate | Result |
|------|--------|
| Full VBA compile (forced by first `Run`) | PASS (no errors) |
| CFAlloc_SelfTest | PASS |
| SSR_CompileProbe / SSR_SelfTest | RAN / PASS |
| CF_FastModuleRegression | PASS \| INPUT_COLUMN_A_GATE=PASS \| CF=USER_HANDOFF |
| CFX_RunAllReplay | REPLAY_ALL_PASS \| 28/28 \| FAIL=0 |
| CF_Offline_CompileProbe | PASS \| INPUT_SHEET=CF_CONTROL |
| CF_InputWorklist_CompileProbe | RAN (WL constants 9/200/17 valid) |
| CF_InputLayout_CompileProbe | RAN |
| CF_Phase3_RunUnitTests | 11/11 PASS. SAP Write=0 |

Module replace confirmed: `modCF_InputWorklist` 320→319, `modCF_InputLayout`
590→592 code lines; VBA component count 73→73 (replaced, not added).
`INPUT_COLUMN_A_GATE=PASS` confirms the OB input anchor at column A survives the
+2 worklist shift.

## Engine finding (answers "phần group vẫn bỏ sót" for V3.0)

- `modCFGroupAllocator` is byte-identical V2.7 ↔ V3.0 — not the source of "bỏ sót".
- V3.0 `modCF_MONCore_STABLE` already uses the V2.7-stable **non-lazy** grid read:
  `RowCount` + `For r = 0 To rows-1` + `GetCellValue(row, colKey)` — reads by
  logical row index against the full count, independent of scroll position.
- CFX replay 28/28 proves the group/allocation logic is sound offline.

**Conclusion:** V3.0 already contains the "kết cấu chạy ổn" from V2.7 for the two
things that matter (allocation logic + non-lazy grid read). A verbatim port of the
V2.7 engine is **not** needed and would regress V3.0's own group fix in
`modCodexWTRework`.

**Live caveat (not faked):** "bỏ sót" is ultimately live-SAP behavior. Offline
gates are green; a real MON session must still confirm the WT grid is fully
materialized before read. Status = OFFLINE_PASS, LIVE_NOT_YET_PROVEN.
