# V2.7 ITERATION LOG

> Chronological record of the V2.7 work, keyed to the user's 8-step roadmap
> (order fixed 2026-08-20). Each step lists what was done, the harness/script, and evidence.

## Roadmap order (authoritative)

1 Fix Allocation Engine → 2 Regression Qty/group → 3 Split CHECK/XỬ LÝ →
4 Single MON/scheduler → 5 Holder Release PLAN_ONLY → 6 History V2 →
7 20→5 sheets → 8 UI final.

Ordering principle (user): UI + sheet optimization go LAST, so a bug can't be
ambiguously attributed to new logic vs sheet migration.

## Bước 1 — Fix Allocation Engine ✅

- Built `modCFGroupAllocator` (CONTRACT_A ≠ CONTRACT_B); installed `RequiredCreateQty = max(TargetQty − EffectiveQtyAfterPlannedCancelReplace, 0)`; dropped the `GroupTarget−GroupCurrent` driver.
- Script: `apply_alloc_patch.ps1`, `cp2_run_regression.ps1`. Evidence: `cp2_alloc_regression.log` G01 totCreate=3500; `CFAlloc_SelfTest=PASS`.

## Bước 2 — Regression Qty/group ✅

- 3 independent tiers green: allocator 11/11 + `CF_FastModuleRegression` PASS + `CF_Phase3_RunUnitTests` 11/11 (SAP Write=0).
- Confirmed the fix doesn't break replace/cancel/create (R01–R07, G02–G04).

## Bước 3 — Split CHECK / XỬ LÝ ✅ (offline)

- Discovery: V2.6 already had CF_Check read-only + PlanID/fingerprint/hash-lock. Real defect = FAST path wrote SAP without verifying the lock (Seam 2).
- Fix: reused existing `CWR_AssertQueuePlanLock` in `CWR_RunFastToCFCore` before create. Script: `cp3_apply_seam2.ps1`; verify `cp3_verify.ps1`. Evidence: `cp3_verify.log` (LOCK_BEFORE_CREATE, 7 guard calls), `cp3_planlock_test.log`.
- Abandoned an early redundant `modCFExecContext` (would have rebuilt existing infra — violates additive-only rule). Never saved into the COPY.

## Bước 4 — Single MON Broker + Scheduler ✅ (offline)

- Broker `CWR_GetMONSession` already correct. Hardened dead legacy `CWR_ReuseVerifiedPRDOTabForMON` to fail-closed `STOP_LEGACY_MON_REUSE_DISABLED`. Script: `cp4_harden_mon.ps1`. Re-verify green.

## Bước 5 — Holder Release PLAN_ONLY ✅ (offline) + LIVE blocked

- `modReleaseHolderReader` complete/read-only/PLAN_ONLY; eligibility excludes A/C correctly. 15 `RELEASE_MON_*` maps empty → fail-closed `STOP_RELEASE_LAYOUT_UNPROVEN` (needs live SAP GUI ID discovery). BLOCKED_EXTERNAL.

## Bước 6 — History V2 ✅ (core, offline)

- `CF_HistoryEnsureLedger`/`CF_HistoryAppend` verified: RUN_HISTORY 27 cols, 3304 rows; hierarchical RunID (CheckID→ExecuteID→ActionID). 27→65 col expansion = enhancement, deferred.

## Bước 7 — 20→5 sheets + remove POSTING 🟡 (GATED)

- Documented 8-step POSTING-removal protocol + 5-sheet fold map. POSTING has 3 runtime referrers; destructive half GATED pending `SourceStockRepository` (needs live POSTING data). See `V2.7_MIGRATION_AUDIT.md`.

## Bước 8 — UI final 🟡 (logic DONE, shapes deferred)

- Built + saved `modCFResetUI` (LÀM MỚI). `CFResetUI_SelfTest=PASS`. Mapped 4 buttons; hide 3 diagnostics. Physical shape rearrangement deferred (cosmetic, last). Script: `cp8_resetui.ps1`. See `V2.7_UI_DECISIONS.md`.

## Bước 9 — Package deliverables ✅ (this step)

- Copied `WORKING\...CP2_ALLOC.xlsm` → `OUTPUT\260820_CF_OB_V2.7.xlsm` (hash-identical). Generated 11 deliverable docs + ZIP + FINAL_AUDIT. Final status `OFFLINE_PASS_LIVE_BLOCKED_EXTERNAL`.

## Notable corrections during the work

- **Quota exhaustion:** a subagent regression run hit API 429; recovered by running regression **directly via local Excel COM** (immune to API quota). Lesson recorded in memory.
- **Redundant module:** `modCFExecContext` abandoned once the architecture map showed V2.6 already had the equivalent — reused existing code instead.
