# V2.7 REGRESSION CATALOG

> The catalog of every offline regression case run for V2.7, what it proves, and
> where the evidence lives. Live-only cases are listed with status `BLOCKED_EXTERNAL`
> (no fake PASS). Results are in `V2.7_REGRESSION_REPORT.md`.

## Tier A — Allocation engine (allocator unit + Qty/group regression)

Harness: `modCFGroupAllocator.CFAlloc_SelfTest` + `modCFAllocRegression.CFReg_RunAll`
(run via local Excel COM, SAP write = 0). Log: `work\REPORTS\cp2_alloc_regression.log`.

| ID | Case | Proves |
|---|---|---|
| A-ST | CFAlloc_SelfTest | Full-project compile clean; allocator math sane. |
| R01 | NOOP | Target==Sys → CREATE=0, Bpass=True. |
| R02 | DELTA_ADD_20 | KEEP delta → CREATE=20. |
| R03 | DELTA_ADD_400 | KEEP delta → CREATE=400. |
| R04 | DECREASE_REPLACE_400 | REPLACE → effective 0 → CREATE=400. |
| R05 | CANCEL_ZERO | CANCEL → CREATE=0, Bpass=True. |
| R06 | BIN_CHANGE_REPLACE_580 | bin change forces REPLACE → CREATE=580. |
| R07 | REPLACE_INCREASE_640 | REPLACE + increase → CREATE=640. |
| **G01** | **NETZERO_3500_4500** | **Core bug: net-zero aggregate still yields totCreate=3500, complete=False.** |
| G02 | NETZERO_AFTER_CREATE | after Create → complete=True, allocWrite=False, totCreate=0. |
| G03 | MULTI_INCREASE | multi-member increase → totCreate=120, A=False. |
| G04 | MIXED_REALLOC | mixed realloc → totCreate=700, A=True, complete=False. |

## Tier B — FAST module contract regression

Harness: `CF_FastModuleRegression` (offline; fail-closed live paths). Log: `cp3_verify.log`.

| Assertion | Proves |
|---|---|
| INPUT_COLUMN_A_GATE | OB list gate honored. |
| ADOPT = LIVE_PASS_LOCKED | adopt path locked. |
| CREATE_SAVE = LIVE_PASS_LOCKED | create+save path locked. |
| DIRECT_CREATE_AND_SAVE = DISCOVERED_PENDING | discovery, not blind write. |
| POST_CAPTURE_CLASSIFIER = TESTED | readback classifier exercised. |
| CF = USER_HANDOFF | no auto-confirm. |

## Tier C — Phase 3 unit tests

Harness: `CF_Phase3_RunUnitTests`. Log: `cp3_verify.log`.

| Assertion | Proves |
|---|---|
| UNIT TESTS 11/11 PASS | phase-3 planner/executor units pass. |
| SAP Write = 0 | zero SAP mutation during unit run. |

## Tier D — CHECK/EXECUTE plan-lock (Seam 2)

Harness: `cp3_verify.ps1` static order check + `cp3_test_planlock.ps1`.
Logs: `cp3_verify.log`, `cp3_planlock_test.log`, `cp3_planlock_state.log`.

| Assertion | Proves |
|---|---|
| FAST_FN_RANGE 1292–1332, LOCK_AT=1300, CREATE_AT=1318 | lock precedes create in FAST. |
| FAST_ORDER_OK = LOCK_BEFORE_CREATE | ordering invariant holds. |
| ASSERTQUEUEPLANLOCK_CALLS = 7 | guard wired at all executor entries. |
| PLAN lock cells consistent | AJ2=LOCKED, AK2=runID, AN2/AO2 hashes intact. |
| tamper detected | mutating a lock cell is observable (re-CHECK forced). |

## Tier E — Single MON Broker hardening

Harness: `cp4_harden_mon.ps1` + re-verify. Log: `cp4_harden_mon.log`.

| Assertion | Proves |
|---|---|
| LEGACY_MON_REUSE_DISABLED | legacy reuse fn fail-closed. |
| 3-tier re-verify green | hardening didn't regress A/B/C. |

## Tier F — LIVE ladder (BLOCKED_EXTERNAL — SAP GUI not installed)

| ID | Case | Status |
|---|---|---|
| T03 | live CHECK read-only | BLOCKED_EXTERNAL |
| T04 | live net-zero group execute | BLOCKED_EXTERNAL |
| T05 | live replace/cancel | BLOCKED_EXTERNAL |
| T08 | live holder release readback | BLOCKED_EXTERNAL |
| T09 | live idempotency/resume | BLOCKED_EXTERNAL |
| FIX | fixture 5042889690 stale-state | BLOCKED_EXTERNAL |
| HRM | holder release matrix (16 case) live | BLOCKED_EXTERNAL |

Max attainable offline status: `OFFLINE_PASS_LIVE_BLOCKED_EXTERNAL`.
