# V2.7 FINAL AUDIT

> Verified integrity + functional audit of the packaged output workbook.
> Evidence: `work\REPORTS\v27_final_audit.log` (static ZIP/XML + live Excel COM compile proof, ReadOnly, no-save). Run 2026-08-20.

## Final status

```
OFFLINE_PASS_LIVE_BLOCKED_EXTERNAL
```

Engineering + package complete; live SAP flows could not be executed (SAP GUI Scripting
not installed). No live result invented.

## 1. File integrity

| Check | Result |
|---|---|
| Output file | `260820_CF_OB_V2.7.xlsm` |
| SHA-256 | `3ba75a86b97b0155f61c15abb32d998b348909424268f5683a34080514c590b0` |
| Size | 2,595,812 bytes |
| Hash vs WORKING copy | identical (clean copy, no re-save drift) |
| ZIP entries | 55 |
| `xl/vbaProject.bin` present | ✅ |
| `xl/workbook.xml` present | ✅ |
| `[Content_Types].xml` present | ✅ |
| XML parse | OK |

## 2. Workbook

| Check | Target | Actual | Note |
|---|---|---|---|
| Sheet count (XML) | 5 | **20** | migration GATED (see MIGRATION_AUDIT) |
| Sheet count (live) | — | 20 | matches XML |
| VeryHidden sheets | — | 7 | |
| Hidden sheets | — | 0 | |
| Only INPUT visible (user mode) | yes | **not yet** | physical UI deferred |
| PLAN!AJ2 lock | LOCKED | LOCKED | ✅ |
| PLAN!AT2 contract | CHECK_LOCKED_V2 | CHECK_LOCKED_V2 | ✅ |

The 5-sheet / single-visible targets are intentionally **not met** — they are the GATED
destructive migration + deferred cosmetic UI. This is disclosed, not a silent miss.

## 3. VBA

| Check | Result |
|---|---|
| Components | 67 (65 base + `modCFGroupAllocator` + `modCFResetUI`) |
| `modCFGroupAllocator` present | ✅ |
| `modCFResetUI` present | ✅ |
| Compile PASS (full project) | ✅ `CFAlloc_SelfTest=PASS` — VBA compiles the whole project before running any procedure, so a passing Run proves a clean compile |
| `CFResetUI_SelfTest` | ✅ `PASS|PLAN_LOCK_INVALIDATED=True|HISTORY_PRESERVED=TRUE|SAP_WRITE=0` |
| Duplicate old button entry paths | none introduced |
| New runtime legacy sheet references | none introduced (POSTING refs unchanged from baseline, gated) |

## 4. Functional

| Property | Result |
|---|---|
| CHECK read-only | ✅ (CF_Check has no `.Press`/`.Text=`/`FindById`) |
| Stale-plan stop | ✅ FAST now asserts lock before create (LOCK_BEFORE_CREATE, 7 guard calls) |
| Member allocation | ✅ CONTRACT_B (RequiredCreateQty) |
| Net-zero group case | ✅ G01 totCreate=3500, complete=False |
| Replace case | ✅ R04/R06/R07 |
| Holder PLAN_ONLY | ✅ fail-closed `STOP_RELEASE_LAYOUT_UNPROVEN` |
| Idempotency / recovery | ✅ offline units; live BLOCKED_EXTERNAL |
| No auto CF | ✅ `CF=USER_HANDOFF` |

## 5. Performance / footprint

| Metric | Value |
|---|---|
| MON navigation | budgeted via `CWR_ReserveMON*`; DOCUMENT_OPEN_COUNT target = 1 |
| Per-P reopen | eliminated (Single MON Broker; legacy reuse fail-closed) |
| Worksheet footprint before/after | 20 → 20 (migration gated) |
| History rows | RUN_HISTORY 3304 (27 cols) |

## 6. Blocked external gates (honest ledger)

| Gate | Why blocked |
|---|---|
| Live ladder T03/T04/T05/T08/T09 | SAP GUI Scripting not installed |
| Fixture 5042889690 stale-state | needs live reproduction |
| 15 `RELEASE_MON_*` maps | need live SAP GUI element-ID discovery |
| POSTING removal + 20→5 merge | needs `SourceStockRepository` built from live POSTING data, then zero-reference proof |
| Physical 4-button UI shapes | cosmetic, deferred to last per ordering principle |

## 7. The 10 evidence questions (PROMPT §24)

1. **Root cause of net-zero bug:** driver used `GroupTarget − GroupCurrent`; when a group's ups and downs net to zero, delta=0 → `NO_QTY_WRITE`, dropping a still-required member Create.
2. **`PRDO_QTY_ALREADY_TARGET` after fix:** it no longer gates the whole group as complete; it only asserts the PRDO **aggregate** (CONTRACT_A). Member Creates are now driven independently by CONTRACT_B.
3. **RequiredCreateQty for KEEP_EXISTING_CREATE_DELTA:** `max(TargetQty − SysQty, 0)`.
4. **RequiredCreateQty for REPLACE_CANCEL_CREATE:** `max(TargetQty − 0, 0) = TargetQty` (effective existing qty treated as 0).
5. **3500+4500→0+8000 replay:** CONTRACT_A passes (aggregate already 8000), CONTRACT_B requires a 3500 Create on the short member → action sequence keeps the Create (totCreate=3500), group not complete until verified.
6. **`STOP_PRDO_DOCUMENT_MEMBER_NOT_INCLUDED` false-positive:** avoided — members are evaluated by allocation contract, not by whether they appear in one specific PRDO save.
7. **MON tabs per EXEC run:** target 1 dedicated MON reader (DOCUMENT_OPEN_COUNT=1); navigation budgeted; offline harness performs 0 live opens.
8. **CANCEL_OTHER_WT / RELEASE_MON_*:** reader complete + read-only + PLAN_ONLY; **no** RELEASE_MON_* map is LIVE-verified (all 15 pending live ID discovery) → fail-closed.
9. **UI buttons / visible sheets:** logic = 4 buttons defined + 3 diagnostics to hide; physical shapes + single-visible-sheet NOT yet applied (deferred). Disclosed.
10. **Static/replay PASS vs needs-LIVE:** PASS static/replay = all Tier A–E (allocation 11/11, FAST module, Phase-3 11/11, plan-lock order, MON hardening). Needs LIVE = Tier F (T03/T04/T05/T08/T09, fixture 5042889690, holder matrix).
